import { createRoot } from "react-dom/client";
createRoot(document.getElementById("root")!).render(
  <div style={{color:"lime",background:"#111",padding:20,fontFamily:"monospace"}}>
    React is mounting correctly
  </div>
);
